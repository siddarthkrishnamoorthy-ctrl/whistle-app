// Same create wizard as the organizer portal, embedded in the admin panel.
// After creation it lands on the tournament's /organizer manage page — the
// admin creates, then the work moves to the organizer side.
export { default } from "@/app/organizer/new/page";
